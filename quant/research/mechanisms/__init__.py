"""Compiled HUNTER theses. Each module is one Mechanism subclass.

Modules in this package are auto-imported by `Mechanism._import_all_mechanisms()`
so any class decorated with `@register("thesis_id")` becomes available
throughout the rest of the system.
"""
